import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.michivito.app',
  appName: 'Mi Chivito',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;